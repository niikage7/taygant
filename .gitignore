# Инструкции для Claude Code — локальные, в репозиторий не идут.
/CLAUDE.md
/.claude/

# Спецификация API (backend/api-spec.yml) намеренно НЕ игнорируется —
# это общий контракт с фронтендом и должна лежать в репозитории.

# Go
/backend/backend
/backend/main
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
go.work
go.work.sum
out/images.tar.gz

# Переменные окружения
.env
.env.local
*.local

# Astra Linux (ЗПС): конфиг развёртывания и оффлайн-артефакты.
# Ключи подписи по умолчанию живут в ~/taygant/keys (вне репозитория).
.env.astra
/astra-offline/
/astra/offline/out/

# Редакторы / ОС
.idea/
.vscode/
.DS_Store
Thumbs.db
out~/images.tar.gz
